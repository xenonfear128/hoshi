# 探针第一、二阶段实施与验收

实施依据：[设计草案](agent-monitoring.md)。目标是完整实现第二阶段，以下项目在有证据前均不算完成。

- [x] 独立 Linux amd64/arm64 Agent：非特权采集、配置、能力状态、服务运行
- [x] 基础资源、逐核 CPU、挂载点、逐磁盘 I/O、网卡速率/错误/丢弃、GPU
- [x] ICMP/TCP/HTTP 网络质量任务与加权历史
- [x] 单次注册、独立凭证、撤销/轮换、HTTPS/WSS/POST、重连/有界缓冲/去重
- [x] 新数据库迁移、账号隔离、元数据加密、验证与限流
- [x] 持续历史、聚合、峰值、流量账期、断档、重启事件
- [x] 离线/恢复、持续阈值、流量告警、迟滞/静默/冷却、站内/Webhook
- [x] 持久安装任务、SSH 指纹/平台/权限预检、幂等安装、首次上报确认
- [x] 自动/手动安装、暂停/恢复、升级/轮换/撤销/卸载
- [x] 添加主机可选开关，主机卡片、完整监控页、工作台来源切换
- [x] 中文/English/日本語、手机/平板/桌面验证
- [x] 单元/集成/负面安全测试、生命周期端到端测试、构建
- [ ] 上线迁移、健康与真实页面检查、最终范围审计

## 证据

2026-09-11：确认原监控依赖活动 SSH 订阅；原数据库迁移有校验和约束，新功能必须追加迁移。

2026-09-11：`go test -race ./...`、`go vet ./...`、前端构建/10 项单元测试、监控页 32 项 Playwright（三语言、明暗主题、360–1440 宽度）通过；一次性 Linux systemd fixture 完成真实 SSH/SFTP 安装、重复任务幂等、升级新会话确认、暂停/恢复、凭证轮换和卸载清理验收。剩余未完成项只有生产上线与线上健康检查。
